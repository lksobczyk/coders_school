head -n1 /etc/issue

sudo apt update
sudo apt upgrade

sudo apt install doublecmd-common
sudo apt install doublecmd-gtk

sudo apt install g++
#sudo apt install clang
sudo apt install cmake
# make / nmake
sudo apt install git
sudo apt install vim
# nano / nautilus / dolphin

which g++
g++ --version
make --version
cmake --version


g++ palindrome.cpp -o palindrome.out
./palindrome.out


git config --global user.email "lksobczyk@wp.pl"
git config --global user.name "Lukasz"

git clone https://github.com/lksobczyk/kurs_cpp_podstawowy.git
cd kurs_cpp_podstawowy/

git checkout -b calculate
cd module1/homework/calculate

mkdir build
cd build
cmake ..
make
ctest -V
git commit -am"the first cohomology"
git push origin calculate

git checkout master
git checkout -b fibonacci
cd module1/homework/fibonacci

mkdir build
cd build
cmake ..
make
ctest -V
git commit -am"the second cohomology"
git push origin fibonacci



cd Projects/kurs_cpp_podstawowy/
git checkout master
git remote add coders https://github.com/coders-school/kurs_cpp_podstawowy.git
git fetch coders
git status

git remote remove coders

git checkout module2
git branch -vv
git remote -v

git push origin module2

git checkout -b addeven
git push origin addeven

git checkout -b nwdnww
git push origin nwdnww

git checkout -b maxvector
git push origin maxvector

git checkout -b gensequence
git push origin gensequence

